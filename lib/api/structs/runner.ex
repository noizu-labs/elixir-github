# Generated by `mix github.gen` from docs/github-api/api.github.com.json.
# Do not edit by hand; re-run the task instead.

defmodule Noizu.Github.Runner do
  @moduledoc false
  defstruct [
    :busy,
    :ephemeral,
    :id,
    :labels,
    :name,
    :os,
    :runner_group_id,
    :status,
    :version
  ]

  def from_json(json, headers \\ [])
  def from_json(nil, _headers), do: nil

  def from_json(list, headers) when is_list(list) do
    Enum.map(list, &from_json(&1, headers))
  end

  def from_json(json, _headers) when is_map(json) do
    %__MODULE__{
      busy: Map.get(json, :busy),
      ephemeral: Map.get(json, :ephemeral),
      id: Map.get(json, :id),
      labels: Enum.map(Map.get(json, :labels) || [], &Noizu.Github.RunnerLabel.from_json(&1)),
      name: Map.get(json, :name),
      os: Map.get(json, :os),
      runner_group_id: Map.get(json, :runner_group_id),
      status: Map.get(json, :status),
      version: Map.get(json, :version)
    }
  end

  def from_json(other, _headers), do: other
end
