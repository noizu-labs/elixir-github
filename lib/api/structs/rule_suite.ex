# Generated by `mix github.gen` from docs/github-api/api.github.com.json.
# Do not edit by hand; re-run the task instead.

defmodule Noizu.Github.RuleSuite do
  @moduledoc false
  defstruct [
    :actor_id,
    :actor_name,
    :after_sha,
    :before_sha,
    :evaluation_result,
    :id,
    :pushed_at,
    :ref,
    :repository_id,
    :repository_name,
    :result,
    :rule_evaluations
  ]

  def from_json(json, headers \\ [])
  def from_json(nil, _headers), do: nil

  def from_json(list, headers) when is_list(list) do
    Enum.map(list, &from_json(&1, headers))
  end

  def from_json(json, _headers) when is_map(json) do
    %__MODULE__{
      actor_id: Map.get(json, :actor_id),
      actor_name: Map.get(json, :actor_name),
      after_sha: Map.get(json, :after_sha),
      before_sha: Map.get(json, :before_sha),
      evaluation_result: Map.get(json, :evaluation_result),
      id: Map.get(json, :id),
      pushed_at: Map.get(json, :pushed_at),
      ref: Map.get(json, :ref),
      repository_id: Map.get(json, :repository_id),
      repository_name: Map.get(json, :repository_name),
      result: Map.get(json, :result),
      rule_evaluations: Map.get(json, :rule_evaluations)
    }
  end

  def from_json(other, _headers), do: other
end
