# Generated by `mix github.gen` from docs/github-api/api.github.com.json.
# Do not edit by hand; re-run the task instead.

defmodule Noizu.Github.GitTag do
  @moduledoc false
  defstruct [
    :message,
    :node_id,
    :object,
    :sha,
    :tag,
    :tagger,
    :url,
    :verification
  ]

  def from_json(json, headers \\ [])
  def from_json(nil, _headers), do: nil

  def from_json(list, headers) when is_list(list) do
    Enum.map(list, &from_json(&1, headers))
  end

  def from_json(json, _headers) when is_map(json) do
    %__MODULE__{
      message: Map.get(json, :message),
      node_id: Map.get(json, :node_id),
      object: Map.get(json, :object),
      sha: Map.get(json, :sha),
      tag: Map.get(json, :tag),
      tagger: Map.get(json, :tagger),
      url: Map.get(json, :url),
      verification: Noizu.Github.Verification.from_json(Map.get(json, :verification))
    }
  end

  def from_json(other, _headers), do: other
end
