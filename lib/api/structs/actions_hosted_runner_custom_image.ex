# Generated by `mix github.gen` from docs/github-api/api.github.com.json.
# Do not edit by hand; re-run the task instead.

defmodule Noizu.Github.ActionsHostedRunnerCustomImage do
  @moduledoc false
  defstruct [
    :id,
    :latest_version,
    :name,
    :platform,
    :source,
    :state,
    :total_versions_size,
    :versions_count
  ]

  def from_json(json, headers \\ [])
  def from_json(nil, _headers), do: nil

  def from_json(list, headers) when is_list(list) do
    Enum.map(list, &from_json(&1, headers))
  end

  def from_json(json, _headers) when is_map(json) do
    %__MODULE__{
      id: Map.get(json, :id),
      latest_version: Map.get(json, :latest_version),
      name: Map.get(json, :name),
      platform: Map.get(json, :platform),
      source: Map.get(json, :source),
      state: Map.get(json, :state),
      total_versions_size: Map.get(json, :total_versions_size),
      versions_count: Map.get(json, :versions_count)
    }
  end

  def from_json(other, _headers), do: other
end
