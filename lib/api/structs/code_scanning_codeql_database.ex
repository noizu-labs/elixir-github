# Generated by `mix github.gen` from docs/github-api/api.github.com.json.
# Do not edit by hand; re-run the task instead.

defmodule Noizu.Github.CodeScanningCodeqlDatabase do
  @moduledoc false
  defstruct [
    :commit_oid,
    :content_type,
    :created_at,
    :id,
    :language,
    :name,
    :size,
    :updated_at,
    :uploader,
    :url
  ]

  def from_json(json, headers \\ [])
  def from_json(nil, _headers), do: nil

  def from_json(list, headers) when is_list(list) do
    Enum.map(list, &from_json(&1, headers))
  end

  def from_json(json, _headers) when is_map(json) do
    %__MODULE__{
      commit_oid: Map.get(json, :commit_oid),
      content_type: Map.get(json, :content_type),
      created_at: Map.get(json, :created_at),
      id: Map.get(json, :id),
      language: Map.get(json, :language),
      name: Map.get(json, :name),
      size: Map.get(json, :size),
      updated_at: Map.get(json, :updated_at),
      uploader: Noizu.Github.SimpleUser.from_json(Map.get(json, :uploader)),
      url: Map.get(json, :url)
    }
  end

  def from_json(other, _headers), do: other
end
