# lib/api/ — Generated API Surface

Generated by `mix github.gen` from `docs/github-api/api.github.com.json`.
Do not edit by hand — re-run the generator instead.

```
lib/api/
├── <category>/<category>.ex   # 49 API modules — Noizu.Github.Api.<Category>
└── structs/
    ├── <schema>.ex            # 826 object-schema structs — Noizu.Github.<Schema>
    └── collection/<item>.ex   # 110 typed list wrappers — Noizu.Github.Collection.<Item>
```

## Category modules (`Noizu.Github.Api.<Category>`)

One module per OpenAPI tag, one function per operation, dispatching through
`Noizu.Github.api_call/5`. `owner`/`repo` come from options or app config; other
path params are positional function arguments.

```
actions            agent_tasks        agents            api_insights
apps               billing            campaigns         checks
classroom          code_quality       code_scanning     code_security
codes_of_conduct   codespaces         copilot           copilot_spaces
credentials        dependabot         dependency_graph  emojis
enterprise_team_memberships  enterprise_team_organizations  enterprise_teams
gists              git                gitignore         hosted_compute
interactions       issues             licenses          markdown
meta               migrations         oidc              orgs
packages           private_registries projects          pulls
rate_limit         reactions          repos             search
secret_scanning    security_advisories  teams           users
```

## Structs (`Noizu.Github.<Schema>`)

826 permissive structs, one per object schema, each with `from_json/2`. Grouped
by name prefix (counts are approximate):

| Prefix group | ~Count | Examples |
| --- | --- | --- |
| `webhook` / `webhooks` | 307 | `webhook_*`, `webhooks_*` (event payloads/configs) |
| `repository` / `simple` | 61 | `repository`, `simple_user`, `repository_*` |
| `code` | 35 | `code_scanning_*`, `code_of_conduct` |
| `actions` | 28 | `actions_secret`, `actions_cache_*`, `runner` |
| `nullable` | 23 | `nullable_*` (nullable variants of base schemas) |
| `secret` | 21 | `secret_scanning_*` |
| `organization` / `org` | 20 | `organization`, `org_*` |
| `projects` | 14 | `projects_*` |
| `issue` | 14 | `issue`, `issue_*` |
| `pull` | 11 | `pull_request`, `pull_*` |
| `dependabot` | 9 | `dependabot_*` |
| `timeline`, `deployment`, `billing`, `team`, `copilot`, `check` | 7–8 each | … |
| other (`review`, `commit`, `workflow`, `content`, `gist`, `branch`, …) | 1–6 each | … |

Fallback for unmapped objects: `Noizu.Github.Raw`.

## Collections (`Noizu.Github.Collection.<Item>`)

110 typed list wrappers carrying pagination links (parsed via
`Noizu.Github.extract_links/1`). Grouped by prefix: `code` (8),
`organization`/`issue` (5 each), `team`/`simple`/`repository`/`pull`/`projects`/
`org`/`gist` (3 each), and smaller groups. Fallback for unmapped list responses:
`Noizu.Github.Collection`.
